# Copy to `.env` and fill in real values. The `.env` file is gitignored.
# Used by `pdfsys_bench.llm_client` for data eval / annotation calls
# against any OpenAI-compatible chat-completions endpoint.

OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
